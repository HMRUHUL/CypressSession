{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "cypress": "^13.13.3"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.8"
  }
}
